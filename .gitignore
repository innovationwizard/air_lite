# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/

# Exception: Don't ignore CDK and frontend source files
!infra_cdk/lib/**/*.ts
!infra_cdk/bin/**/*.ts
!frontend/src/lib/

# But DO ignore compiled files
infra_cdk/**/*.js
infra_cdk/**/*.d.ts
!infra_cdk/jest.config.js
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Secrets - NEVER COMMIT THESE
*.pem
*.key
credentials.json
.aws/
*.env
!example.env
# Catch every dotenv variant (.env, .env3, .env.prod, …) so no secrets leak.
.env*
!.env.example
!**/.env.example
.env.local
.env.*.local
**/.env.local
**/.env.*.local

# Node.js (for CDK)
node_modules/
.npm/
npm-debug.log*
cdk.context.json
cdk.out/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# SQLite
*.db
*.sqlite
*.sqlite3

# Data files - NEVER COMMIT BUSINESS DATA
*.csv
*.xlsx
*.xls
*sample*data*/
*_archive/
*_discard/
db_credentials

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Ignore build output and cdk artifacts
/dist
/cdk.out
# Client data — never commit
real_data/

# Supabase local
supabase/.temp/

# Large raw Odoo extracts and prod backups — too big for git
docs/reconciliation/odoo_extract_*.json
docs/reconciliation/backup_*.json

# Documentation / working notes — kept local, not version-controlled
docs/

# Client-provided working drop 2026-08-04 (Alexis xlsx + meeting notes) — local only
20260804/

# Tech-debt golden-backtest harness + client-derived forecast baselines — local only
ml/_baselines/

# Python test/coverage artifacts
.coverage
.coverage.*
.pytest_cache/
htmlcov/
